#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <cassert>
#include <memory>

#define CHAR_MAX_COUNT 10000

static int dequeue_count = 0;
static int error = 0;

namespace ip3
{
    class formatter
    {
    private:
        /* data */
    public:
        formatter(/* args */);
        ~formatter();
    };

    formatter::formatter(/* args */)
    {
    }

    formatter::~formatter()
    {
    }

}