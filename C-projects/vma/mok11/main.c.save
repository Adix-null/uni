#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <limits.h>
#include <float.h>
#include <stdbool.h>
#include <assert.h>
#include <string.h>

#include "file.h"

int main()
{
    int data_[ARRAY_SIZE] = {0};
    int data_read[ARRAY_SIZE] = {0};
    char* file_name = "inp.txt";

    fillArray(data, ARRAY_SIZE, 0, 73);
    printArray(data, ARRAY_SIZE);
    saveToFile(file_name, data, ARRAY_SIZE);
    


    return 0;
}
